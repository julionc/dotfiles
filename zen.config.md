# Zen config (firefox)

## Main Tab Unloader Flags in about:config

`browser.tabs.min_inactive_duration_before_unload`:
Sets the time in milliseconds an inactive tab waits before unloading (default is 600000 for 10 minutes).

`browser.tabs.fadeOutUnloadedTabs`: Set to true to visually dim tabs that have been put to sleep.


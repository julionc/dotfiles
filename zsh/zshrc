
source ~/.dotfiles/zsh/prezto.zsh

# Customize to your needs...
for conf (~/.dotfiles/zsh/conf.d/*(N.)) source $conf
#for func (~/.dotfiles/zsh/func.d/*(N.)) autoload -U ${func:t}

# zsh_private
if [ -e "$HOME/.zsh_private" ]; then
  source "$HOME/.zsh_private"
fi

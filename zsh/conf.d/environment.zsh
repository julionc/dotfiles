#
# Variables
#

export EDITOR=vim
export VISUAL=subl

# rbenv
export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init -)"

[ -s $HOME/.nvm/nvm.sh ] && . $HOME/.nvm/nvm.sh  # This loads NVM

# vim: set ft=zsh:
